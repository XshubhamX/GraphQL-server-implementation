type Query {
  users(idQuery: ID!, nameQuery: String!): [User!]!
  repos(idQuery: ID!, nameQuery: String!): [Repo!]!
}

type User {
  id: ID!
  name: String!
  email: String!
}

type Repo {
  id: ID!
  title: String!
  visibility: String!
}
